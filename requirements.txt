# Core dependencies
argparse
rich
setuptools
numpy
pyyaml

# HuggingFace backend (default)
huggingface_hub
accelerate>=0.26.0
transformers[torch]>=4.38.0
torch>=2.7.0
bitsandbytes>=0.43.0

# Ollama backend (optional)
ollama>=0.1.0

# Note: For CUDA 12.8+ support (required for RTX 5090 / Blackwell):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# For older GPUs (RTX 30xx/40xx) CUDA 12.4 also works:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
